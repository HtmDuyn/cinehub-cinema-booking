# # 1. Cấu hình Cổng
# server.port=8080

# # 2. Tên ứng dụng
# spring.application.name=CineHub-Backend

# # 3. Cấu hình Database (ĐÃ FIX CHO MÁY BẠN)
# # Dùng 127.0.0.1 để tránh lỗi Connection Refused
# spring.datasource.url=jdbc:sqlserver://127.0.0.1:1433;databaseName=cinehub_db;encrypt=true;trustServerCertificate=true

# # Username & Password (ĐÃ FIX)
# spring.datasource.username=sa
# spring.datasource.password=12345
# # (Nếu máy bạn pass là 12345 thì sửa lại số 1 ở trên, còn nãy mình nhớ là 1)

# # 4. Cấu hình JPA
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect

# # 5. CẤU HÌNH GỬI MAIL (BẠN PHẢI TỰ ĐIỀN VÀO ĐÂY)
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=ngcongtoan.309@gmail.com

# # Mật khẩu 16 ký tự lấy từ Google (App Password), KHÔNG PHẢI mật khẩu đăng nhập
# spring.mail.password=osns isuu jgca susa
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

# # Format SQL cho dễ đọc 
# spring.jpa.properties.hibernate.format_sql=true 
# # Bật log chi tiết câu SQL 
# logging.level.org.hibernate.SQL=DEBUG 
# # Bật log giá trị bind vào câu SQL (tham số ?) 
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
# 1. Cấu hình Server
server.port=8080
spring.application.name=CineHub-Backend

# 2. Database
spring.datasource.url=jdbc:sqlserver://127.0.0.1:1433;databaseName=cinehub_db;encrypt=true;trustServerCertificate=true
spring.datasource.username=sa
spring.datasource.password=12345

# 3. JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Bật log SQL chi tiết
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Dialect cho SQL Server
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect

# 4. Mail (bạn tự điền App Password Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=cinehub.noreply@gmail.com
# Mật khẩu 16 ký tự lấy từ Google (App Password), KHÔNG PHẢI mật khẩu đăng nhập
spring.mail.password=momz ifia zuox dmog
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
